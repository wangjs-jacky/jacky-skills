# {{PROJECT_NAME}}

{{PROJECT_DESC}}

## 工作原理

1. GitHub Actions 按照 `{{CRON_SCHEDULE}}` 定时执行（或手动触发）
2. 执行 `scripts/collect.mjs` 采集数据
3. 执行 `scripts/process.mjs` 处理数据（可选 AI 处理）
4. 自动提交更新到仓库

## 快速开始

### 1. 克隆并配置

```bash
git clone https://github.com/{{OWNER}}/{{REPO}}.git
cd {{REPO}}
npm install
```

### 2. 配置 Secrets

在 GitHub 仓库设置中添加以下 Secrets：

| Secret | 用途 | 必需 |
|--------|------|------|
| `GITHUB_TOKEN` | GitHub API 访问 | ✅ 自动提供 |
{{#SECRETS}}
| `{{SECRET_NAME}}` | {{SECRET_DESC}} | {{SECRET_REQUIRED}} |
{{/SECRETS}}

### 3. 本地测试

```bash
# 运行采集脚本
node scripts/collect.mjs

# 运行处理脚本
node scripts/process.mjs

# 运行测试
node scripts/__tests__/collect.test.mjs
```

### 4. 手动触发 Workflow

```bash
gh workflow run collect.yml
```

### 5. 查看运行日志

```bash
gh run watch
```

## 目录结构

```
{{REPO}}/
├── .github/
│   └── workflows/
│       └── collect.yml      # GitHub Actions Workflow
├── scripts/
│   ├── collect.mjs           # 数据采集脚本
│   ├── process.mjs           # 数据处理脚本
│   └── __tests__/
│       ├── collect.test.mjs  # 采集测试
│       └── process.test.mjs  # 处理测试
├── output/                    # 采集的数据存储
│   ├── data.json            # 原始数据
│   ├── processed.json       # 处理后的数据
│   └── .collected           # 已采集条目（去重用）
├── README.md                # 本文件
├── package.json             # Node.js 配置
└── .env.example             # 环境变量示例
```

## 配置说明

### Cron 调度

编辑 `.github/workflows/collect.yml` 中的 cron 表达式：

```yaml
schedule:
  - cron: '*/30 * * * *'  # 每 30 分钟
```

常用 cron 表达式：

| 表达式 | 含义 |
|--------|------|
| `*/15 * * * *` | 每 15 分钟 |
| `0 * * * *` | 每小时 |
| `0 0 * * *` | 每天 00:00 |
| `0 9 * * 1-5` | 工作日 09:00 |

### AI 处理配置

如果启用了 AI 处理，需要配置：

1. 在 GitHub Secrets 中添加 `OPENAI_API_KEY`
2. 编辑 `scripts/process.mjs` 中的 `AI_PROMPT` 来自定义处理逻辑

### 自定义采集逻辑

编辑 `scripts/collect.mjs` 中的 `collectData()` 函数来适配你的数据源。

## 测试

```bash
# 运行所有测试
node scripts/__tests__/collect.test.mjs
node scripts/__tests__/process.test.mjs

# 或使用 npm test（如果配置了）
npm test
```

## 故障排查

### Workflow 没有运行

1. 检查 GitHub Actions 是否启用
2. 检查 Secrets 是否正确配置
3. 查看 Workflow 运行日志

### 数据采集失败

1. 检查数据源 URL 是否可访问
2. 检查 API 密钥是否有效
3. 查看 `output/` 目录下的日志

### AI 处理失败

1. 检查 `OPENAI_API_KEY` 是否配置
2. 检查 API 配额是否用尽
3. 查看 Workflow 日志中的错误信息

## License

MIT
